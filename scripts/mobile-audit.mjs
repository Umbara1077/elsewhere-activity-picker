import {chromium} from '@playwright/test';const b=await chromium.launch({channel:'msedge'});const p=await b.newPage({viewport:{width:390,height:844},isMobile:true,hasTouch:true});await p.goto('http://127.0.0.1:5173/');await p.waitForTimeout(1800);console.log(await p.evaluate(()=>({width:innerWidth,scroll:document.documentElement.scrollWidth,small:[...document.querySelectorAll('input,select')].map(e=>({label:e.getAttribute('aria-label'),font:getComputedStyle(e).fontSize})),overflow:[...document.querySelectorAll('body *')].filter(e=>{const r=e.getBoundingClientRect();return r.width>0&&r.right>innerWidth+2&&getComputedStyle(e).position!=='absolute'}).slice(0,12).map(e=>e.className)})));await p.screenshot({path:'scripts/mobile-before.png',fullPage:true});await b.close();
